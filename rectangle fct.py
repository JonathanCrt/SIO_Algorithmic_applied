def rectangle (a,b,c):
    pyth=b*b*+c*c
    if a*a = pyth :
        return 1
    else :
        return 0


#Programme principal :
a=float (input('a ?'))
b=float (input('b ?'))
c=float (input('c ?'))
print ("Il y a ",rectangle(a,b,c),"rectangle")
